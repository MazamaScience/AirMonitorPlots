legend_pm25Timeseries <- function() {
  
  list(
    # Add some more margin space above the plot
    theme(
      plot.margin = margin(
        t = unit(6.0 * 11, "pt")
        ),
      plot.title = element_text(
        vjust = 20
      )
    )
    
  )
 
  
  # Add annotation for legend
  # Using a bunch of 'grid' stuff...
  
}  
